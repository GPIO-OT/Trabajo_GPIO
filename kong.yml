_format_version: "3.0"

services:
  - name: backend-service
    url: http://backend:5000
    routes:
      - name: backend-route
        paths:
          - /api
plugins:
  - name: key-auth
    service: backend-service

  - name: file-log
    config:
      path: /dev/stdout
